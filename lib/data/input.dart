class UserInput {
  final int angle;
  final int xOffset;

  UserInput({
    required this.angle,
    required this.xOffset,
  });
}

class MergedInput {
  final int angle;
  final int xOffset;
  final int yOffset;

  MergedInput({
    required this.angle,
    required this.xOffset,
    required this.yOffset,
  });
}
